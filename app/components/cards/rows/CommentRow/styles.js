import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginBottom: 30,
  },
  heading: {
    fontWeight: 'bold',
    marginBottom: 15,
  },
  comment: {
    fontStyle: "italic",
    marginBottom: 10,
  },
  rating: {
    fontStyle: "italic",
    fontSize: 12,
  }
});

export default styles;
